<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            
        }

        .container {
            width: 100%;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .form-container {
            justify-content: center;
            align-items: center;
            width: 350px;
            height: 400px;
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0px 1px 10px rgba(0, 0, 0, 0.1);
            transition: 2s;
        }
        .container:hover{
            transform: scale(1.02);
        }

        input[type="text"], input[type="password"] {
                width: 90%;
                margin-bottom: 10px;
                border: 1px solid #ddd;
                outline: none;
                border-radius: 3px;
                padding: 8px;;
                font-size:1rem;
        }

        .submit-btn {
            width: 90%;
            padding: 8px;
            margin-top: 10px;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            font-size:1rem;

        }
        .form-container .submit-btn{
            align-items: center;
            justify-content: center;
            margin-left: 1rem;
        }
        .form-container input[type="text"], input[type="password"] {
            align-items: center;
            justify-content: center;
            margin-left: .5rem;
        }

        .submit-btn:hover {
            background-color: #255d28;
        }
        .form-img-container{
            justify-content: center;
            align-items: center;
            width: 350px;
            height: 440px;
            border-radius: 5px;
            box-shadow: 0px 1px 10px rgba(0, 0, 0, 0.1);
            transition: 2s;
            
        }
        @media screen and (max-width:780px){
            .container {
                display: flex;
                flex-direction: column;
                width: 100%;
            }
            .form-img-container img{
                height: 440px;
                width: 100%;
                margin: auto;
            }
            .form-container{
                padding: 0;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="form-img-container">
            <img src="forgot.avif" alt="" height="440px" width="350px">
        </div>
        <div class="form-container">
            <h2 style="text-align:center; color:#357f39;font-size:2rem" >Forgot Password</h2>
            <form method="post" action="/forgot">
                <input type="text" name="email" placeholder="Enter Your Email ID" >
                <button type="submit" class="submit-btn">Send Password Reset Link</button>
                </a>                
                <button type="submit"  class="submit-btn"><a href="/login" style="text-decoration: none; color:white;">Back To Login</a></button>
                <p>{{resetLink}}</p>
            </form>
        </div>
    </div>
</body>
</html>